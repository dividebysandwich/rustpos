#!/bin/bash
set -e

# Stop service before removal
if systemctl is-active --quiet rustpos.service; then
    systemctl stop rustpos.service || true
fi

# Disable service on full removal (not upgrade)
if [ "$1" = "remove" ]; then
    systemctl disable rustpos.service || true
fi
